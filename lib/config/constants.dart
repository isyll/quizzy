import 'package:flutter/material.dart';

class AppConstants {
  static const appName = 'Quizzy';
  static const locales = [
    Locale('en'),
    Locale('fr'),
  ];
  static const defaultLocale = Locale('en');
}
